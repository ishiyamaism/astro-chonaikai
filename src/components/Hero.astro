---
const url_hero_image = "/images/hero.webp";
const backgroundImageStyle = {
  backgroundImage: `url(${url_hero_image})`,
  backgroundSize: "cover",
  backgroundPosition: "center",
};
---

<div
  style={backgroundImageStyle}
  class="bg-cover bg-center bg-fixed flex flex-col items-center justify-center min-h-[360px] h-[calc(100vh-200px)]"
>
  <div class="bg-white/30 py-2 px-4 rounded-xl text-center backdrop-blur-md">
    <div class="relative p-2">
      <img src={"/images/logo.webp"} width="250px" height="250px" alt="logo" />
      <div class="flex items-center justify-center mt-[-195px] mr-[88px] mb-5">
        <div class="w-14 h-14 flex items-center justify-center gap-1">
          <div class="h-3 w-1 bg-slate-200 rounded-full animate-wavey"></div>
        </div>
      </div>
    </div>
    <div class="text-5xl font-bold text-zinc-200 mt-[145px] pb-2">
      並林町内会
    </div>
  </div>
  <div class="font-bold mt-3 text-md text-zinc-200">
    広報誌と回覧板を今すぐ購読する
  </div>
  <div class="mt-3 mx-auto w-auto flex gap-2 px-2">
    <input
      type="email"
      placeholder="Email"
      class="rounded-sm border border-white/40 bg-white/30 backdrop-blur-md p-2 font-bold text-amber-100 placeholder-zinc-200 caret-amber-500 outline-amber-700 focus:outline w-2/3"
    />
    <button
      class="cursor-pointer rounded-md bg-amber-700 py-2 text-zinc-50 font-bold transition-colors hover:bg-amber-600 hover:shadow-lg hover:shadow-black/20 w-1/3"
    >
      購読
    </button>
  </div>
</div>
